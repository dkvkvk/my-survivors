# 法宝与技能系统设计（P6）

> 本文是**唯一权威设计文档**，改设计先改这里。最后更新：2026-09-22（补第 5/6 把法宝 + 精确斩妖归属）

## 一、已确认的规则

### ★ 模型 B：一把法宝 = 一个被动效果 + 一组技能（2026-09-21 定稿）

| | 说明 |
|---|---|
| **被动效果** | 自动生效，**等级 = 法宝品阶**（一~五阶）。捡到法宝立刻生效，丢掉立刻卸下 |
| **技能** | 法宝**提供**技能；每场只能选其中一个当"当前激活技能"，占用对应技能槽 |
| **满级进化** | 法宝升到 `max_level`（5）时自动进入进化形态（数值强化，见 `balance.gd` 的 `*_EVOLVE_*`） |
| **等级来源** | 唯一来源是 `player.weapons[i]["level"]`，由 `player._apply_weapon_passive()` 分发给法宝节点 |

> 之前（P5 阶段）被动效果挂在"法宝卡"上，而法宝卡已移出三选一卡池，
> 导致**捡到法宝只有技能槽、被动完全不生效**（含"雷神之怒"空技能）。
> 模型 B 把被动和法宝绑死，从根上修掉这个问题。

当前 **6 把法宝**（法宝位仍是 4 —— 种类多于位子，"带哪几件"才有选择）：

| 法宝 | id | 被动（等级 = 法宝品阶） | 技能 | 升级材料 |
|---|---|---|---|---|
| 本命飞剑 | `shuriken` | 自动投掷：每 2 级 +1 弹丸/+1 伤害，每级 +10% 射速 | 万剑归宗 / **御剑疾影** | 玄铁 |
| 周天剑环 | `orbit_blade` | 刀刃环绕自身，刀刃数 = 等级 | 剑气纵横 / **剑环外放** | 玄铁 |
| 离火法环 | `aura` | 周期灼烧周围敌人，半径/伤害随等级 | 大日焚天 / **焚地火域** | 雷魄 |
| 连环雷符 | `chain_lightning` | 命中后在敌人间跳跃，跳跃数/伤害随等级 | 九天神雷 / **蓄雷引弧** | 雷魄 |
| 回风梭 | `boomerang` | 掷梭飞出后折返，去程/返程各能命中一次；每 2 级 +1 枚 | 风卷残云 / **穿云巨梭** | 玄铁 |
| 地火符阵 | `mine` | 身周布符雷，妖近即爆（范围伤害）；每 2 级 +1 张在场 | 十方雷网 / **符阵合围** | 雷魄 |

**身份与起手（P7 多角色）**：本命飞剑是**所有身份共有的基础法宝**；身份额外决定"签名法宝"与属性——守山人（无签名，均衡）、**符修**（地火符阵起手，灵力 ×1.4、血量 ×0.75）、**剑修**（周天剑环起手，射速 ×1.25、移速 ×0.9）。定义在 `characters.gd`，主菜单选择并存档。

**融合神通（2026-09-24 实装）**：两把法宝**同时在携带列表里**时，它们各自都会多出第 3 门可选神通：

| 融合神通 | 需要同时携带 | 效果 |
|---|---|---|
| 焚天剑轮 `inferno_ring` | 周天剑环 + 离火法环 | 带火的剑环外放 + 原地留一片火域 |
| 惊雷剑引 `storm_volley` | 本命飞剑 + 连环雷符 | 放射剑雨 + 天雷当空劈落 |

可选范围只有一处判定：`Skills.available_for(player, weapon_id)` —— 乾坤袋显示与
`player.set_active_skill` 都走它，"能不能选"不会两处不一致。

> 回风梭与地火符阵的节点、造型、图标全部**代码生成**（沿用刀光/电弧的纯代码先例，
> 见 `tools/gen_weapon_icons.py`）；想换成 AI 生图，按 `XIANXIA_ART_PROMPTS.md` 的
> "P6 补充图标"提示词生成后覆盖同名文件即可。

### 技能
- 神通共 **14 门**（6 把法宝 ×2 + 2 门融合），数据在 `skills.gd`
- **4 个技能槽**，键位 **1 / 2 / 3 / 4**（不用 QWER——W 已被移动占用）
- 施放**消耗蓝条**（上限 100，每秒回 6，见 `balance.gd` 的 `MANA_*`）
- 每个技能有**独立冷却**；冷却中按键无效、蓝不够时 HUD 变暗
- 技能数据在 `skills.gd`；技能效果在 `player._run_skill_effect()`
- **技能槽顺序 = 法宝顺序**（`player._sync_skill_slots()`）；换法宝会重排槽位

### 法宝
- **上限 4 把**（与 4 个技能槽一一对应；法宝**种类**已有 6 种，"换哪把"是真实取舍）
- 升级途径 **B + C 都要**：消耗**材料** 且 **斩妖数**累积
  - 材料：`base_cost × 当前等级`
  - 斩妖：`kills_per_level × 当前等级`
- **"与技能相关"的定义（方案 A）**：法宝**提供**技能；不提供任何已用技能的法宝算"不相关"
- 数据表 `weapons.gd`：id / 名称 / 图标 / passive / 技能列表 / max_level / 升级成本

### 拾取
- 法宝掉在地上 → 走近提示 → **按 F 拾取**（不自动捡）
- 法宝位满了又捡 → 弹**替换面板**（暂停），可**替换**或**不换**
- 重复法宝 → 转成 2 个升级材料

### 开局选法宝（2026-09-21 新增，2026-09-22 改为"1 固定 + 3 随机"）
- 进战斗时弹 4 张卡并**暂停游戏**，选 1 把再开打（`start_select_ui.gd`）
- **本命飞剑固定占一张**（没有它就没有自动攻击，开局会没法玩）：
  选它 = 起手直接给到 `Balance.START_WEAPON_LEVEL`（3 级：弹丸 2 发、伤害 +1）
- 其余 3 张从**另外 5 把法宝**里随机抽（种类 6 > 法宝位 4，全列也会超出面板宽度）
- 选非本命飞剑 = 追加装备（法宝位 +1、技能槽 +1），所以开局就有 **2 种攻击方式**

### 掉落来源（2026-09-21 调整）
- 普通怪：**12%** · 妖王：**必掉**
  > 原为 4%。实测站桩 3 分钟只杀得掉 66 只 → 期望 2.6 把、实际 0 把，
  > 玩家反馈"角色只有一种攻击方式"，所以提高基础概率并加开局保底。
- **开局保底**：前 `WEAPON_PITY_TIME` 秒内，每 `WEAPON_PITY_KILLS` 次斩妖还没掉够法宝就必掉一把（最多 `WEAPON_PITY_MAX` 把）
- 地上同时最多留 `WEAPON_DROP_MAX_GROUND` 把，超了回收最早的（防满地掉落物、也防节点堆积）
- 重复法宝 → 转成 2 个升级材料

### 藏宝匣奖励（P6 重做）
- 50% 材料礼包（玄铁 x8 + 雷魄 x3）
- 20% 免材料免斩妖，直接给一把**还没满级**的已持有法宝 +1 级
- 其余 30% 灵石 15~25

### 升级三选一
- 卡池**只出属性卡**（`upgrades.gd` 5 张），法宝卡已移出

---

## 二、乾坤袋设计（按 B 打开）

> 需求：目前没有乾坤袋功能，按 B 打开。因为法宝只有 4 格、材料是计数，
> **不需要网格乾坤袋**（那是给上百件物品用的），做一个**专用面板**更合适。

### 布局

`@
┌──────────────────── 乾坤袋（B 开关，暂停游戏）────────────────────┐
│  法宝 (4/4)                     │  当前技能配装                  │
│ ┌──────┐┌──────┐┌──────┐┌──────┐│   [1] 万剑归宗               │
│ │法宝A ││法宝B ││法宝C ││ 空   ││   [2] 刃风暴                   │
│ │3阶   ││1阶   ││2阶   ││      ││   [3] —                        │
│ └──────┘└──────┘└──────┘└──────┘│   [4] —                        │
│                                  │                                │
│  选中法宝的技能（每场选 1 个）    │  材料                          │
│  ● 万剑归宗   (当前激活)       │   玄铁 x12  雷魄 x3            │
│  ○ 影分身                        │                                │
│  ○ 千鸟                          │  灵力 80/100                   │
│                                  │                                │
│  升级：玄铁 x5 雷魄 x1  已用 47/100 次斩妖  [升级]                │
└──────────────────────────────────────────────────────────────────┘
`@

### 交互
| 操作 | 效果 |
|---|---|
| **B** | 开关乾坤袋（暂停游戏；升级/结算界面打开时忽略） |
| **Esc** | 关闭乾坤袋（开着时；没开时才是暂停菜单） |
| 点法宝格 | 选中该法宝，右侧显示它的全部技能与升级信息 |
| 点技能行 | 把该技能设为这把法宝**本场的激活技能** → 占用对应的技能槽 |
| 点 [升级] | 材料够且斩妖数够时，消耗并 +1 级 |

### 数据模型
`@gdscript
# weapons.gd
const LIST := [
  {"id":"shuriken", "name":"本命飞剑", "icon":"...", "max_level":5,
   "passive":"gun",                       # 被动类型，player 按此分发
   "skills":["shuriken_burst"],
   "upgrade_material":"scrap", "base_cost":5, "kills_per_level":100},
]

# player.gd
var weapons: Array = []        # 最多 4 项：{"id":..., "level":1, "active_skill":..., "kills":0}
var materials: Dictionary = {} # {"scrap":12, "crystal":3}
`@

---

## 三、GitHub 调研结果（开源参考）

| 仓库 | ★ | 说明 | 对本项目的价值 |
|---|---|---|---|
| [DarkRewar/SurvivorsStarterKit](https://github.com/DarkRewar/SurvivorsStarterKit) | 104 | Godot 4 幸存者类起步套件（C#，3D） | **Powerups 数据驱动**思路：每个强化是一个 `.tres` 资源。**无乾坤袋系统** |
| [expressobits/inventory-system](https://github.com/expressobits/inventory-system) | 736 | Godot 4 模块化乾坤袋（节点式） | 若将来要网格乾坤袋可参考；**当前需求用不上** |
| [alfredbaudisch/GodotDynamicInventorySystem](https://github.com/alfredbaudisch/GodotDynamicInventorySystem) | 655 | 动态乾坤袋 + 无限滚动 UI | 同上，UI 思路可参考 |
| [don-tnowe/godot-wyvernbox-inventory](https://github.com/don-tnowe/godot-wyvernbox-inventory) | 163 | ARPG 向乾坤袋（Godot 3/4） | 装备/词条思路可参考 |
| [migvalm/vampire-survivors-clone](https://github.com/migvalm/vampire-survivors-clone) | 27 | Godot 4 幸存者克隆 | 玩法循环参考 |

**结论**：没有现成的"幸存者类 + 法宝技能选择"乾坤袋可以直接抄。
主流乾坤袋插件都是**网格 + 拖拽**的重型方案，本项目只有 4 格法宝 + 计数材料，
**自建专用面板**最合适（轻、好改、风格统一）。参考其"数据与 UI 分离"的做法即可。

---

## 四、待办（TODO）

### 🔜 技能融合（已确认推迟）
- 两个技能同时装备时可融合成融合技能
- `skills.gd` 里已放 `FUSIONS` 占位表
- 需要补：融合 UI、融合效果、融合占 1 槽还是 2 槽

### 主待办
- [x] `weapons.gd` 法宝数据表
- [x] 乾坤袋面板（按 B）+ 法宝 4 格 + 技能选择 + 材料 + 升级按钮
- [x] 法宝掉落物实体（按 F 拾取 + 走近提示）
- [x] 法宝上限 4 + 替换面板
- [x] 法宝升级（材料 + 斩妖数）
- [x] `upgrades.gd` 拆分：属性卡留三选一，法宝卡移出
- [x] **模型 B：法宝被动接入**（`_apply_weapon_passive` / 满级自动进化）
- [x] **雷神之怒从空技能改成"多道天雷"**
- [x] **开局选法宝（4 选 1）** + 掉落率 4%→12% + 开局保底
- [x] **斩妖经验精确归属**（`take_damage` 带 `source` → `died(source)` → 只记致命一击的法宝）
- [x] **第 5 / 6 种法宝**：回风梭（`boomerang`）/ 地火符阵（`mine`）——替换面板从此在实战生效
- [x] **给法宝补第 2 门神通**（2026-09-24）：6 把法宝各 2 门 + 2 门融合，共 14 门
- [ ] 技能特效（见第六节）

### 已知限制 / 后续
- **替换面板已可达**（2026-09-22）：法宝种类 6 > 法宝位 4，拾到未持有的第 5 把即弹替换面板。
- **开局抽卡**：4 张 = 本命飞剑 + 随机 3 张；想看全 6 把法宝得靠掉落 / 藏宝匣。
- **斩妖归属（2026-09-22 已精确化）**：`mob.take_damage(amount, knockback, source)` 的 `source` 由各法宝传入
  （`gun`/飞刀/光环/闪电/新法宝各自填自己的 id），死亡时 `died(source)` → `player.add_kill_credit(source)`，
  **只有造成致命一击的那把法宝** +1 斩妖数；`source` 为空串（来源不明）时兜底记给所有携带法宝。
  因为一把法宝现在只能拿到自己补刀的那部分，`weapons.gd` 的 `kills_per_level` 已按比例下调（35/40/40/50）。
- **换神通要消耗神通残卷**：首次激活不耗，改换另一门消耗 1 本（掉落率很低），
  所以"带哪门神通"是有代价的取舍，不是随时免费切。

---

## 五、当前实现进度

| 模块 | 状态 |
|---|---|
| 蓝条 + 回蓝 | ✅ |
| 4 技能槽（键位 1234）+ 冷却 + 耗蓝 | ✅ |
| 技能 HUD（冷却遮罩/空槽变暗/蓝不够变暗） | ✅ |
| 4 个技能效果（万剑归宗 / 刃风暴 / 烈日爆发 / 雷神之怒） | ✅（雷神之怒 = 多道天雷） |
| 开局自带本命飞剑 → 自带技能 | ✅ |
| 法宝数据表 `weapons.gd`（含 passive 字段） | ✅ |
| **法宝被动 = 法宝品阶**（4 把全部接通） | ✅ |
| 满级自动进化 | ✅ |
| 乾坤袋（B）+ 法宝 4 格 + 技能选择 + 材料 + 升级按钮 | ✅ |
| 法宝掉落 + 按 F 拾取 + 替换面板 | ✅ |
| 材料掉落（玄铁/雷魄）+ 神通残卷掉落 | ✅ |
| 斩妖数累积（法宝升级条件） | ✅ |
| 升级三选一只出属性卡 | ✅ |
| 藏宝匣奖励改走法宝/材料体系 | ✅ |
| 技能融合 | ⬜ 推迟 |

---

## 六、需要出的图

| # | 文件名 | 尺寸 | 用途 | 状态 |
|---|---|---|---|---|
| 1 | `skill_shuriken.png` | 64×64 | 万剑归宗 | ✅ 已接入 |
| 2 | `skill_orbit_blade.png` | 64×64 | 刃风暴 | ✅ 已接入 |
| 3 | `skill_aura.png` | 64×64 | 烈日爆发 | ✅ 已接入 |
| 4 | `skill_chain_lightning.png` | 64×64 | 雷神之怒 | ✅ 已接入 |
| 5 | `icon_weapon_drop.png` | 32×32 | 地上掉落的法宝 | ✅ 已接入 |
| 6 | `icon_material_scrap.png` | 32×32 | 升级材料·玄铁 | ✅ 已接入 |
| 7 | `icon_material_crystal.png` | 32×32 | 升级材料·雷魄 | ✅ 已接入 |
| 8 | `icon_skill_book.png` | 32×32 | 神通残卷 | ✅ 已接入 |

> 规格：洋红底 `#FF00FF`、单图标、`crisp pixels`、修仙+修仙（青霓虹 + 红木/和纸）
> 下一批美术需求（特效贴图等）见 `ART_PROMPTS.md`。
