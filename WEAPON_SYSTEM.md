# 武器与技能系统设计（P6）

> 本文是**唯一权威设计文档**，改设计先改这里。最后更新：2026-09-21（模型 B 定稿）

## 一、已确认的规则

### ★ 模型 B：一把武器 = 一个被动效果 + 一组技能（2026-09-21 定稿）

| | 说明 |
|---|---|
| **被动效果** | 自动生效，**等级 = 武器等级**（Lv1~5）。捡到武器立刻生效，丢掉立刻卸下 |
| **技能** | 武器**提供**技能；每场只能选其中一个当"当前激活技能"，占用对应技能槽 |
| **满级进化** | 武器升到 `max_level`（5）时自动进入进化形态（数值强化，见 `balance.gd` 的 `*_EVOLVE_*`） |
| **等级来源** | 唯一来源是 `player.weapons[i]["level"]`，由 `player._apply_weapon_passive()` 分发给武器节点 |

> 之前（P5 阶段）被动效果挂在"武器卡"上，而武器卡已移出三选一卡池，
> 导致**捡到武器只有技能槽、被动完全不生效**（含"雷神之怒"空技能）。
> 模型 B 把被动和武器绑死，从根上修掉这个问题。

当前 4 把武器：

| 武器 | 被动（等级 = 武器等级） | 技能 | 升级材料 |
|---|---|---|---|
| 手里剑 | 自动投掷：每 2 级 +1 弹丸/+1 伤害，每级 +10% 射速 | 手里剑乱舞 | 铁屑 |
| 环形刀刃 | 刀刃环绕自身，刀刃数 = 等级 | 刃风暴 | 铁屑 |
| 灼热光环 | 周期灼烧周围敌人，半径/伤害随等级 | 烈日爆发 | 雷晶 |
| 链式闪电 | 命中后在敌人间跳跃，跳跃数/伤害随等级 | 雷神之怒 | 雷晶 |

### 技能
- **4 个技能槽**，键位 **1 / 2 / 3 / 4**（不用 QWER——W 已被移动占用）
- 施放**消耗蓝条**（上限 100，每秒回 6，见 `balance.gd` 的 `MANA_*`）
- 每个技能有**独立冷却**；冷却中按键无效、蓝不够时 HUD 变暗
- 技能数据在 `skills.gd`；技能效果在 `player._run_skill_effect()`
- **技能槽顺序 = 武器顺序**（`player._sync_skill_slots()`）；换武器会重排槽位

### 武器
- **上限 4 把**（当前阶段；与 4 个技能槽一一对应）
- 升级途径 **B + C 都要**：消耗**材料** 且 **击杀数**累积
  - 材料：`base_cost × 当前等级`
  - 击杀：`kills_per_level × 当前等级`
- **"与技能相关"的定义（方案 A）**：武器**提供**技能；不提供任何已用技能的武器算"不相关"
- 数据表 `weapons.gd`：id / 名称 / 图标 / passive / 技能列表 / max_level / 升级成本

### 拾取
- 武器掉在地上 → 走近提示 → **按 F 拾取**（不自动捡）
- 武器位满了又捡 → 弹**替换面板**（暂停），可**替换**或**不换**
- 重复武器 → 转成 2 个升级材料

### 开局选武器（2026-09-21 新增）
- 进战斗时弹 4 张卡并**暂停游戏**，选 1 把再开打（`start_select_ui.gd`）
- **手里剑是固定基础武器**（没有它就没有自动攻击，开局会没法玩）：
  选它 = 起手直接给到 `Balance.START_WEAPON_LEVEL`（3 级：弹丸 2 发、伤害 +1）
- 选其它 3 把 = 追加装备（武器位 +1、技能槽 +1），所以开局就有 **2 种攻击方式**

### 掉落来源（2026-09-21 调整）
- 普通怪：**12%** · 首领：**必掉**
  > 原为 4%。实测站桩 3 分钟只杀得掉 66 只 → 期望 2.6 把、实际 0 把，
  > 玩家反馈"角色只有一种攻击方式"，所以提高基础概率并加开局保底。
- **开局保底**：前 `WEAPON_PITY_TIME` 秒内，每 `WEAPON_PITY_KILLS` 次击杀还没掉够武器就必掉一把（最多 `WEAPON_PITY_MAX` 把）
- 地上同时最多留 `WEAPON_DROP_MAX_GROUND` 把，超了回收最早的（防满地掉落物、也防节点堆积）
- 重复武器 → 转成 2 个升级材料

### 宝箱奖励（P6 重做）
- 50% 材料礼包（铁屑 x8 + 雷晶 x3）
- 20% 免材料免击杀，直接给一把**还没满级**的已持有武器 +1 级
- 其余 30% 金币 15~25

### 升级三选一
- 卡池**只出属性卡**（`upgrades.gd` 5 张），武器卡已移出

---

## 二、背包设计（按 B 打开）

> 需求：目前没有背包功能，按 B 打开。因为武器只有 4 格、材料是计数，
> **不需要网格背包**（那是给上百件物品用的），做一个**专用面板**更合适。

### 布局

`@
┌──────────────────── 背包（B 开关，暂停游戏）────────────────────┐
│  武器 (4/4)                     │  当前技能配装                  │
│ ┌──────┐┌──────┐┌──────┐┌──────┐│   [1] 手里剑乱舞               │
│ │武器A ││武器B ││武器C ││ 空   ││   [2] 刃风暴                   │
│ │Lv3   ││Lv1   ││Lv2   ││      ││   [3] —                        │
│ └──────┘└──────┘└──────┘└──────┘│   [4] —                        │
│                                  │                                │
│  选中武器的技能（每场选 1 个）    │  材料                          │
│  ● 手里剑乱舞   (当前激活)       │   铁屑 x12  雷晶 x3            │
│  ○ 影分身                        │                                │
│  ○ 千鸟                          │  法力 80/100                   │
│                                  │                                │
│  升级：铁屑 x5 雷晶 x1  已用 47/100 次击杀  [升级]                │
└──────────────────────────────────────────────────────────────────┘
`@

### 交互
| 操作 | 效果 |
|---|---|
| **B** | 开关背包（暂停游戏；升级/结算界面打开时忽略） |
| **Esc** | 关闭背包（开着时；没开时才是暂停菜单） |
| 点武器格 | 选中该武器，右侧显示它的全部技能与升级信息 |
| 点技能行 | 把该技能设为这把武器**本场的激活技能** → 占用对应的技能槽 |
| 点 [升级] | 材料够且击杀数够时，消耗并 +1 级 |

### 数据模型
`@gdscript
# weapons.gd
const LIST := [
  {"id":"shuriken", "name":"手里剑", "icon":"...", "max_level":5,
   "passive":"gun",                       # 被动类型，player 按此分发
   "skills":["shuriken_burst"],
   "upgrade_material":"scrap", "base_cost":5, "kills_per_level":100},
]

# player.gd
var weapons: Array = []        # 最多 4 项：{"id":..., "level":1, "active_skill":..., "kills":0}
var materials: Dictionary = {} # {"scrap":12, "crystal":3}
`@

---

## 三、GitHub 调研结果（开源参考）

| 仓库 | ★ | 说明 | 对本项目的价值 |
|---|---|---|---|
| [DarkRewar/SurvivorsStarterKit](https://github.com/DarkRewar/SurvivorsStarterKit) | 104 | Godot 4 幸存者类起步套件（C#，3D） | **Powerups 数据驱动**思路：每个强化是一个 `.tres` 资源。**无背包系统** |
| [expressobits/inventory-system](https://github.com/expressobits/inventory-system) | 736 | Godot 4 模块化背包（节点式） | 若将来要网格背包可参考；**当前需求用不上** |
| [alfredbaudisch/GodotDynamicInventorySystem](https://github.com/alfredbaudisch/GodotDynamicInventorySystem) | 655 | 动态背包 + 无限滚动 UI | 同上，UI 思路可参考 |
| [don-tnowe/godot-wyvernbox-inventory](https://github.com/don-tnowe/godot-wyvernbox-inventory) | 163 | ARPG 向背包（Godot 3/4） | 装备/词条思路可参考 |
| [migvalm/vampire-survivors-clone](https://github.com/migvalm/vampire-survivors-clone) | 27 | Godot 4 幸存者克隆 | 玩法循环参考 |

**结论**：没有现成的"幸存者类 + 武器技能选择"背包可以直接抄。
主流背包插件都是**网格 + 拖拽**的重型方案，本项目只有 4 格武器 + 计数材料，
**自建专用面板**最合适（轻、好改、风格统一）。参考其"数据与 UI 分离"的做法即可。

---

## 四、待办（TODO）

### 🔜 技能融合（已确认推迟）
- 两个技能同时装备时可融合成融合技能
- `skills.gd` 里已放 `FUSIONS` 占位表
- 需要补：融合 UI、融合效果、融合占 1 槽还是 2 槽

### 主待办
- [x] `weapons.gd` 武器数据表
- [x] 背包面板（按 B）+ 武器 4 格 + 技能选择 + 材料 + 升级按钮
- [x] 武器掉落物实体（按 F 拾取 + 走近提示）
- [x] 武器上限 4 + 替换面板
- [x] 武器升级（材料 + 击杀数）
- [x] `upgrades.gd` 拆分：属性卡留三选一，武器卡移出
- [x] **模型 B：武器被动接入**（`_apply_weapon_passive` / 满级自动进化）
- [x] **雷神之怒从空技能改成"多道天雷"**
- [x] **开局选武器（4 选 1）** + 掉落率 4%→12% + 开局保底
- [ ] 第 5 / 6 种武器（加了这个"替换面板"才会在实战生效）
- [ ] 给武器补多个技能（现在每把只挂 1 个，多技能 UI 已就绪）
- [ ] 技能特效（见第六节）

### 已知限制 / 后续
- **替换面板实战暂不可达**：只有 4 种武器 + 上限 4，重复武器走"转材料"分支。
  新增第 5 种武器后自动生效（逻辑与 UI 已验证）。
- **击杀归属**：目前每击杀给**所有携带武器**各 +1 击杀数（不区分是谁打死的）。
  要精确归属需在 `take_damage` 里带上来源武器 id。
- **武器技能只有 1 个**：`weapons.gd` 里每把武器目前只挂了 1 个技能；
  多技能与"每场选一个"的 UI 已就绪，补技能数据即可生效。

---

## 五、当前实现进度

| 模块 | 状态 |
|---|---|
| 蓝条 + 回蓝 | ✅ |
| 4 技能槽（键位 1234）+ 冷却 + 耗蓝 | ✅ |
| 技能 HUD（冷却遮罩/空槽变暗/蓝不够变暗） | ✅ |
| 4 个技能效果（手里剑乱舞 / 刃风暴 / 烈日爆发 / 雷神之怒） | ✅（雷神之怒 = 多道天雷） |
| 开局自带手里剑 → 自带技能 | ✅ |
| 武器数据表 `weapons.gd`（含 passive 字段） | ✅ |
| **武器被动 = 武器等级**（4 把全部接通） | ✅ |
| 满级自动进化 | ✅ |
| 背包（B）+ 武器 4 格 + 技能选择 + 材料 + 升级按钮 | ✅ |
| 武器掉落 + 按 F 拾取 + 替换面板 | ✅ |
| 材料掉落（铁屑/雷晶）+ 技能切换书掉落 | ✅ |
| 击杀数累积（武器升级条件） | ✅ |
| 升级三选一只出属性卡 | ✅ |
| 宝箱奖励改走武器/材料体系 | ✅ |
| 技能融合 | ⬜ 推迟 |

---

## 六、需要出的图

| # | 文件名 | 尺寸 | 用途 | 状态 |
|---|---|---|---|---|
| 1 | `skill_shuriken.png` | 64×64 | 手里剑乱舞 | ✅ 已接入 |
| 2 | `skill_orbit_blade.png` | 64×64 | 刃风暴 | ✅ 已接入 |
| 3 | `skill_aura.png` | 64×64 | 烈日爆发 | ✅ 已接入 |
| 4 | `skill_chain_lightning.png` | 64×64 | 雷神之怒 | ✅ 已接入 |
| 5 | `icon_weapon_drop.png` | 32×32 | 地上掉落的武器 | ✅ 已接入 |
| 6 | `icon_material_scrap.png` | 32×32 | 升级材料·铁屑 | ✅ 已接入 |
| 7 | `icon_material_crystal.png` | 32×32 | 升级材料·雷晶 | ✅ 已接入 |
| 8 | `icon_skill_book.png` | 32×32 | 技能切换书 | ✅ 已接入 |

> 规格：洋红底 `#FF00FF`、单图标、`crisp pixels`、和风+科技（青霓虹 + 红木/和纸）
> 下一批美术需求（特效贴图等）见 `ART_PROMPTS.md`。
