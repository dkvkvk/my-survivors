# 开发路线图（ROADMAP）

> 最后更新：2026-09-22。本文只维护**批次划分与进度**；已落地功能的权威说明见
> `HANDOVER.md`（环境/坑/验证）、`ARCHITECTURE.md`（代码结构）、`WEAPON_SYSTEM.md`（法宝设计）。

## 已完成

- **批次 0 · 修仙化**：文案与系统命名、图标/地图/角色/背景美术、文档术语清洗全部完成
  （38 张生图入库，实机截图逐张验收）
- **批次 1 · 视觉清晰度**（除妖王轮换）：主角左右朝向修复（镜像右向列，弃用帧间不一致的坏列）、
  妖物可读性（提亮 1.18x + 提饱和 1.08x + 1px 暗描边）、摄像机抖动去掉旋转（只位移）
- **批次 2 · 链式闪电法宝**：连环雷符（被动跳跃电弧 + 技能「九天神雷」+ 满级进化），
  纯代码绘制电弧，零素材依赖
- **L0 loop 判据增强**：新增 `script-parse`（全量 `.gd` 逐个 load，
  补上 `--import` 抓不到「未被场景引用的坏脚本」的盲区），见 `LOOPS.md`

- **批次 4 · 玩法深度（P1）✅ 2026-09-22**
  - 第 5/6 把法宝：**回风梭**（掷出折返，去程/返程各命中一次）/ **地火符阵**（布符雷，妖近即爆）
    —— 法宝种类 6 > 法宝位 4，「法宝掉落 / 替换面板」从此在实战生效
  - **斩妖经验精确归属**：`take_damage(amount, knockback, source)` → `died(source)`，
    只记给造成致命一击的法宝（门槛已按归属比例下调），`tools/qa/check_kill_credit.gd` 守住
  - 开局选法宝改为「本命飞剑固定 + 随机 3 把」，并接入 L0 判定 `weapons-smoke`

- **批次 3 · 移动端（P2）✅ 2026-09-22**
  - **字体子集化**：Fusion Pixel 4.9MB → 46KB（379 字），Web 首载直接省 ~4.8MB；
    判据 `font-coverage` 守住漏字（顺带抓出并修掉了妖王警告的 `⚠` 豆腐块）
  - **触屏操作**：左下虚拟摇杆 + 右下神通按钮，桌面键鼠下完全不存在；
    判据 `touch-controls` + 真实运行探针（`MS_TOUCH_PROBE`）双验证

## 进行中

- **批次 3 收尾 / 批次 5 · 内容量**
  - 竖屏 / 横屏布局适配（手机竖屏现在画面偏小）
  - 多角色（不同初始法宝 / 属性）、多地图主题、成就系统

## 待办批次

- **批次 5 · 内容量**：多角色（不同初始法宝/属性）、多地图主题、成就系统 + 存档扩展
- **批次 6 · 视觉与发布**：妖王多形象轮换（3 种，模板机制已支持）、UI 动效与过场、
  itch.io 发布页（封面 `assets/ui/cover.png` 已备）、BGM 替换

---

## 节奏约定

- 一次只推一个批次，每个功能一个提交，改完即跑 `python tools/qa/loop_judge.py` 判定并更新文档
- 纯代码功能（法宝/系统）与需要生图的功能（美术）可并行，互不阻塞
